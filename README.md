# AutoSuggest-1

verifying my git set-up ;)

changes flow for pushing 
using UI way:
1. add(plus button)
2. commit with message
3. push/syn

using command way:
1. git add -A
2. git commit -m "some message"
3. git push